import React from 'react'
import './Prompt.css'

const Prompt = ({ prompt }) => {
  return (
    <main className='prompt'>
      {/* <h2>{prompt.prompt}</h2>
      <section className='character-details'>
        <h3>Character</h3>
        <p className='character-name'>Name: {character.title} {character.first} {character.last}</p>
        <p>DOB: {date}</p>
        <p className='character-nationality'>Nationality: {this.state.nationality}</p>
      </section>
      <section>
          <h3>Location</h3>
          <p>City: {prompt.city}</p>
          <p>Country: {prompt.country}</p>
        </section>
      <button className='write-prompt-btn'>Write this prompt</button>
      <section className='story-container'>
        STORIESSTORIESSTORIES
      </section> */}
    </main>
  )
}

export default Prompt